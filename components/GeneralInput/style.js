import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    input: {
        width: 250,
        padding: 6,
        margin: 5,
        borderRadius: 8,
        borderColor: '#2a2a2a',
        borderWidth: 1,
    }
})