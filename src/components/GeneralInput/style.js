import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    input: {
        padding: 8,
        marginVertical: 6,
        borderRadius: 8,
        backgroundColor: '#f2f2f2',
        borderColor: '#2a2a2a',
        borderWidth: 1,
    }
})